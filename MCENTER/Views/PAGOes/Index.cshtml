@model IEnumerable<MCENTER.Models.PAGO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table cellspacing="0" cellpadding="0" id="mi-tabla" class="tabla">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tarjeta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecharVencimiento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.titular)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Venta.Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tarjeta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecharVencimiento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.titular)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Venta.Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_pago }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_pago }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_pago })
        </td>
    </tr>
}

</table>
